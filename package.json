{
  "name": "catan-companion",
  "description": "Board generator, rules and cost reference for Catan, Seafarers and Cities & Knights.",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "node scripts/run-tests.mjs",
    "art": "node scripts/generate-hex-art.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vite": "^6.0.0",
    "esbuild": "^0.25.12"
  }
}
